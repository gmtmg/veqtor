# Claude Codeの「エージェント」、正体はただのテキストファイルだった

## この記事について

Claude Codeにはエージェント機能というものがあります。名前だけ聞くと難しそうですが、使ってみたらすごくシンプルでした。

エージェントの正体はただのMarkdownファイルです。「あなたはこういう役割です」と日本語で書いたテキストファイルを置くだけ。コードは一行も書きません。

この記事ではエージェント機能の基礎から、実際にテックブログ運営に使っている事例までをまとめます。

---

## Claude Codeの基本

Claude CodeはAnthropicが出しているターミナル上で動くAIアシスタントです。ブラウザのチャットとは違い、PC上のファイルを直接読み書きできます。

```bash
npm install -g @anthropic-ai/claude-code
```

大事なのは、作業したいフォルダに移動してから起動することです。Claude Codeは「今いるフォルダ」を作業場所として認識します。

```bash
cd ~/my-project
claude
```

あとは日本語で話しかけるだけです。

```
> index.htmlのタイトルを変えて
> style.cssのフォントサイズもう少し大きくして
```

---

## エージェントの正体

ここからが本題です。

エージェントは `.claude/agents/` フォルダに置いた `.mdファイル` です。

```
my-project/
├── .claude/
│   └── agents/       ← ここに.mdファイルを置くだけ
│       ├── writer.md
│       └── reviewer.md
├── index.html
└── ...
```

中身はこんな感じです。

```markdown
# My Agent

## Identity

- name: my-agent
- description: こういうときに使うエージェントです

## 役割

あなたは〇〇の専門家です。△△を担当します。

## 責務

### 1. やること
- 具体的な内容を書く

## 作業手順

1. まず〇〇を確認する
2. 次に△△を実行する
3. 完了したら報告する
```

プログラミング要素はゼロです。日本語で書いた通りにClaude Codeが振る舞ってくれます。使うときは `/agents` で一覧が出てきます。

---

## エージェントを作ってもらう

このファイル自体をClaude Codeに生成してもらえます。こんなプロンプトで頼みます。

### 画像管理エージェント

```
.claude/agents/ に画像管理エージェントを作って。
役割：
- 画像ファイルの命名規則を管理
- サムネイルのサイズ統一（640x360px）
- alt属性が未設定の画像を検出して報告
- どのHTMLからも参照されてない孤立画像をチェック
```

### 記事ライターエージェント

```
記事を書くエージェントを作って。
条件：
- テック系の記事、です・ます調
- 構成は「導入→背景→本論→実践→まとめ」
- 記事のHTMLテンプレートも含めて
```

生成されたファイルはあとから自由に編集できます。最初はざっくり作って、使いながら「ここが足りない」と思ったら書き足す。そのやり方が一番うまくいきました。

---

## 実際のエージェントファイル

自分が使っているものをそのまま載せます。

### image-manager.md

```markdown
# Image Manager

## Identity
- name: image-manager
- description: 画像の管理・最適化担当。

## 役割
あなたはテック・IT系サイトの画像マネージャーです。

## 責務

### 1. 命名規則の管理
- 記事画像: assets/images/articles/{slug}-{連番}.{ext}
- サムネイル: assets/images/thumbnails/{slug}-thumb.{ext}

### 2. alt属性の管理
- 装飾画像: alt=""
- コンテンツ画像: 内容を具体的に記述

### 3. 整合性チェック
- 孤立画像の定期チェック
- 不要画像のリストアップと削除提案
```

普通のMarkdownです。これがそのまま「AIの行動マニュアル」になります。

### article-writer.md（抜粋）

このエージェントにはライターのキャラ設定が含まれています。

```markdown
## ライター設定

| ID      | 名前    | 性格               | 得意分野           |
|---------|---------|--------------------|--------------------|
| compass | Compass | 穏やか・面倒見がいい | AI教材・入門ガイド |
| bolt    | Bolt    | 好奇心旺盛         | 最新ニュース・速報 |
| aegis   | Aegis   | 冷静・慎重           | セキュリティ       |
| prism   | Prism   | 知的好奇心・思索家   | テックコラム       |
| syntax  | Syntax  | 行動派               | プログラミング実践 |
```

テーブルで5人分の設定を書いてあるだけですが、これでキャラごとの書き分けがちゃんと効きます。セキュリティの記事はAegisが冷静な口調で、プログラミング入門はSyntaxが「まず動かしてみよう」というテンションで書いてくれます。

---

## エージェントをチームにする

複数のエージェントを組み合わせると、作業全体を一言で回せます。

「記事を1本公開する」の裏側はこうなっています。

```
① tone-manager    → 文体ルールの確認
② article-writer  → 記事の執筆
③ image-manager   → サムネイル・画像管理
④ seo-optimizer   → SEO最適化
⑤ publisher       → トップページ更新・公開処理
⑥ site-architect  → 全体の整合性チェック
```

これをClaude Codeが順番に処理してくれます。

また、プロジェクトのルートに `CLAUDE.md` を置くと、全エージェント共通のルールを定義できます。命名規則やデプロイ設定など、どのエージェントでも守るべきルールをここに書いておきます。

---

## エージェント設計のコツ

いくつか作ってみてわかったことです。

- **役割は絞る** ― 「なんでもやるエージェント」より1領域に特化させたほうが安定します
- **数値を入れる** ―「titleタグを適切な長さに」より「32〜54文字で」のほうが精度が出ます
- **テンプレートを含める** ― HTMLの雛形を入れておくと、記事間でデザインがブレなくなります
- **作業手順を書く** ―「この順番でやること」を明記すると、ルール無視で作業を始めることがなくなります
- **参照ファイルを明記する** ―「STYLE_GUIDE.mdを読むこと」と書いておくと、作業前に自分で読みに行きます

---

## 実践：テックブログ「Veqtor」

ここからは、エージェントで実際に運営しているブログの話です。

Veqtor（ベクター）はAI・開発・セキュリティなどのテック情報を扱うブログです。

- URL: https://algoyosuga.com/veqtor/
- 静的HTML/CSS（フレームワーク不使用）
- GitHub Actionsで自動デプロイ
- 記事12本（2026年4月時点）

7つのエージェント（site-architect、article-writer、tone-manager、seo-optimizer、image-manager、publisher、ui-designer）と5人のAIライター（Compass、Bolt、Aegis、Prism、Syntax）で運営しています。全部.mdファイルで、合わせても数十KBです。

### 記事を作る流れ

Claude Codeにこう話しかけます。

```
ゼロトラストの入門記事を書いて
```

あとは自動です。文体ルールの確認、既存記事との重複チェック、ライターのアサイン（セキュリティなのでAegis）、記事HTML生成、SEO設定、サムネイル生成、トップページ更新、サイトマップとRSS更新、git push、自動デプロイ。人間がやることは「テーマを伝える」と「できたものを確認する」だけです。

エージェントで記事を作ると、SEO（JSON-LD、OGP、メタディスクリプション）、デザイン（ダーク/ライト対応、レスポンシブ）、文体（ですます調、見出し階層）がすべて自動で標準化されます。12本目でも1本目と同じ品質基準が保たれるので、「メタディスクリプション入れ忘れた」みたいなことが起きません。

---

## おわりに

やっていることを整理するとシンプルです。

1. `.claude/agents/` に.mdファイルを置く
2. 役割・ルール・手順を日本語で書く
3. Claude Codeがその通りに動く

エージェントは最初から完成品を作るより、使いながら育てるほうがうまくいきます。テキストファイルなのでいつでも直せますし、Gitで管理もできます。

興味のある方は、まず1つだけ作って試してみてください。
